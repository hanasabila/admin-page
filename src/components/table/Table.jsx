import { ThemeProvider } from '@mui/material';
import './table.scss'

const Table = () => {
    return (
        <ThemeProvider>
            <div className="table">
                tabel
            </div>
        </ThemeProvider>
       
    );
}

export default Table